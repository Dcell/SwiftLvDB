name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: -*

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Dump file hierarchy
      run: ls -l
    - name: Goin Demo
      run: cd ./swift-lvdb-demo
    - name: Pod install
      run: pwd
    - name: Build
      run: ls -l
    - name: Run tests
      run: ls -l
